import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'frc-scout-module',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
